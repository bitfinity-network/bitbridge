import { BridgeWidget } from './components/BridgeWidget';

function App() {
  /*
  // This is just testing the theme
  const theme: CustomThemeType = {
    colors: {
      main: {
        primary: '#FF6347', // tomato
        hover: '#FFA500', // gold
        secondary: '#FFD700', // gold
        mainBg: '#FFFACD', // lemon chiffon
        modalBg: '#E6E6FA', // lavender
        primaryText: '#8B0000', // dark red
        secondaryText: '#DAA520' // goldenrod
      }
    },
    config: {
      colorMode: 'light'
    }
  };
  */
  return <BridgeWidget />;
}

export default App;
